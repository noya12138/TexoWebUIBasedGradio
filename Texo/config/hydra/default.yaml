job:
  name: bs=${data.train_batch_size}-lr=${training.optimizer.lr}-freeze=${model.encoder.freeze}
run:
  dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
sweep:
  dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
